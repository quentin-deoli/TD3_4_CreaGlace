<Window x:Class="CreaGlace.Options"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CreaGlace"
        mc:Ignorable="d"
        Title="Options" Height="500" Width="800"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="Button" x:Key="BoutonGlace">
            <Setter Property="Background" Value="#FF69B4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="20" 
                                BorderThickness="2"
                                BorderBrush="White">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/glace.jpg" Stretch="UniformToFill"/>
        </Grid.Background>

        <Border Background="White" 
                Opacity="0.90" 
                CornerRadius="30" 
                Width="400" 
                Height="Auto"
                VerticalAlignment="Center" 
                HorizontalAlignment="Center"
                Padding="30">

            <Border.Effect>
                <DropShadowEffect Color="#FF444444" BlurRadius="20" ShadowDepth="5" Opacity="0.3"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock x:Name="parametres" Text="PARAMÈTRES" 
                           Foreground="#FF48CAE4" 
                           FontSize="32" 
                           FontWeight="ExtraBold" 
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,20"
                           FontFamily="Segoe UI Black"/>

                <TextBlock x:Name="volume_musique" Text="Volume Musique" Foreground="#FF555555" FontWeight="Bold" Margin="0,10,0,5"/>
                <Slider x:Name="SliderMusique" Maximum="100" Value="70" TickPlacement="BottomRight" TickFrequency="10" IsSnapToTickEnabled="True"/>

                <TextBlock x:Name="volume_SFX" Foreground="#FF555555" FontWeight="Bold" Margin="0,15,0,5"><Run Text="Volume "/><Run Language="fr-fr" Text="SFX"/></TextBlock>
                <Slider x:Name="SlidersSFX" Maximum="100" Value="50" TickPlacement="BottomRight" TickFrequency="10"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,30,0,0">

                    <Button x:Name="BoutonRetour"
                            Content="Retour" 
                            Style="{StaticResource BoutonGlace}" 
                            Background="#48CAE4" 
                            Width="100"/>

                    <Button x:Name="BoutonSauvegarder"
                            Content="Sauvegarder" 
                            Style="{StaticResource BoutonGlace}" 
                            Width="120"/>
                </StackPanel>

            </StackPanel>
        </Border>
    </Grid>
</Window>