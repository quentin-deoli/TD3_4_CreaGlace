<Window x:Class="CreaGlace.Options"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Options" Height="450" Width="800" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">

    <Window.Resources>
        <!-- Style pour les boutons arrondis -->
        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="Background" Value="#00CFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="10" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SaveButton" BasedOn="{StaticResource RoundedButton}" TargetType="Button">
            <Setter Property="Background" Value="#FF83C2"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Image Source="Glace.jpg" Stretch="Fill"/>

        <Border Width="400" Height="300" Background="#CCFFFFFF" CornerRadius="20" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Margin="20">

                <TextBlock Text="PARAMÈTRES" FontSize="28" FontWeight="Bold" Foreground="#00CFFF" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <!-- Volume Musique -->
                <TextBlock Text="Volume Musique" FontWeight="Bold"/>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider x:Name="SliderMusique" Minimum="0" Maximum="100" Width="250" ValueChanged="SliderMusique_ValueChanged"/>
                    <TextBlock x:Name="txtMusiqueValeur" Text="100%" Width="50" Margin="10,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Volume SFX -->
                <TextBlock Text="Volume SFX" FontWeight="Bold" Margin="0,20,0,0"/>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider x:Name="SliderSFX" Minimum="0" Maximum="100" Width="250" ValueChanged="SliderSFX_ValueChanged"/>
                    <TextBlock x:Name="txtSFXValeur" Text="100%" Width="50" Margin="10,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Boutons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,30,0,0">
                    <Button x:Name="BoutonRetour" Content="Retour" Width="120" Style="{StaticResource RoundedButton}" Margin="0,0,20,0"/>
                    <Button x:Name="BoutonSauvegarder" Content="Sauvegarder" Width="120" Style="{StaticResource SaveButton}"/>
                </StackPanel>

            </StackPanel>
        </Border>
    </Grid>
</Window>
